[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    smoke: critical happy-path tests
    regression: broader functional coverage
    negative: invalid-input scenarios
    boundary: boundary and robustness scenarios
    validation: validation scenarios
